<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/match_parent"
    tools:context="udacity.designvilla.SplashScreenActivity">

    <!-- ImageView for Background Image of the App -->

    <ImageView
        android:id="@+id/splash_ImageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:scaleType="fitXY"
        android:src="@drawable/dv_splash_bg"
        android:contentDescription="@string/bg_image"
        />

    <!-- ImageView for Name of the App -->

    <ImageView
        android:id="@+id/name_ImageView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:src="@drawable/dv_name"
        android:textAlignment="center"
        android:textColor="#000000"
        android:textSize="25sp"
        android:contentDescription="@string/app_name"
        />

</RelativeLayout>
